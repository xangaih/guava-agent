[project]
name = "my-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.141.1",
    "guava-sdk",
    "psycopg[binary]>=3.2",
    "typing_extensions",
    "uvicorn>=0.52.4",
]

[tool.vercel]
entrypoint = "concierge_dashboard:app"
